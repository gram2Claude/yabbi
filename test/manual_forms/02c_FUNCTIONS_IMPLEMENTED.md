# Реестр реализованных функций — {API_NAME}

Этот файл — **шаблон/образец оформления**. По нему Claude ведёт файл
**`info/01_functions_implemented.md`** в корне проекта.

**Когда обновлять:**
- После успешной реализации функции (Шаг 4, после зелёного smoke-теста и
  показа таблицы) — добавить блок о новой функции
- При любых изменениях уже реализованной функции (новые поля, смена endpoint,
  изменение поведения, фикс бага, влияющий на контракт) — обновить
  существующий блок и добавить запись в раздел "История изменений" этого блока

**Цель:** держать актуальную картину "что уже умеет клиент" — какой API-метод
использует каждая публичная функция, какие поля возвращает, какие специфики
важно помнить при использовании и сопровождении.

---

## Функция: `{function_name}`

- **Статус:** реализована
- **Дата реализации:** YYYY-MM-DD
- **Тип:** справочник / статистика / охват
- **Файл:** `{MODULE_NAME}.py` → `{ClassName}.{function_name}`
- **Spec:** `specs/NN_spec_{function_name}.md`
- **Plan:** `plans/NN_plan_{function_name}.md`
- **Smoke-test:** `smoke_tests/test_{function_name}.py`

### API-метод

- **Endpoint:** `{HTTP_METHOD} {ENDPOINT_PATH}`
- **Тип:** sync / async (submit + poll + download)
- **Формат ответа:** `application/json` / `text/csv` / `application/zip` / ...
- **Ссылка на сводку:** см. `info/00_api_methods.md` → раздел `{ENDPOINT_PATH}`

### Параметры функции (Python-сигнатура)

```python
def {function_name}(
    self,
    date_from: str,
    date_to: str,
    campaigns: list[str] | None = None,
    ...,
) -> pd.DataFrame:
    ...
```

### Поля выходного DataFrame

| Колонка (итоговая) | Тип pandas | Источник (поле API) | Описание |
|--------------------|-----------|---------------------|----------|
| `date` | datetime64[ns] | `date` (string YYYY-MM-DD) | Дата отчёта |
| `campaign_id` | string | `campaignId` | Идентификатор кампании |
| `ad_id` | string | `bannerId` (только ad-level) | Идентификатор объявления |
| `ad_name` | string | `bannerName` (только ad-level) | Название объявления |
| `views` | Int64 | `views` (integer) | Показы |
| `clicks` | Int64 | `clicks` (integer) | Клики |
| `costs_nds` | Float64 | `Расход, ₽, с НДС` (BANNER) / `Расход, ₽` (VIDEO_BANNER, без НДС) | Расход — **проверить источник API**, см. ниже; десятичный тип, округление до 2 знаков |
| `costs_without_nds` | Float64 | вычисляется: `costs_nds / (1 + ставка_НДС)` (по году даты: year≥2026 → 1.22, иначе 1.20) | Расход без НДС |
| `ak` | Float64 | константа `0.5` | Агентская комиссия |
| `costs_nds_ak` | Float64 | вычисляется: `costs_nds * (1 + ak)` | Расход с НДС и комиссией |
| `costs_without_nds_ak` | Float64 | вычисляется: `costs_without_nds * (1 + ak)` | Расход без НДС с комиссией |
| `account_id` | Int64 | константа `1` | Пример, заменить при интеграции |
| `source_type_id` | Int64 | константа `9` | Пример |
| `id_key_camp` | string | вычисляется: `"1_" + campaign_id` | Составной ключ кампании |
| `id_key_ad` | string | вычисляется: `id_key_camp + "_" + ad_id` | Составной ключ объявления (**только ad-level**) |

**Для справочника** (вместо части полей выше): добавить ещё `product_id=1`,
`product_name="prod_test"`, `camp_type="camp_test"`, `camp_category="cat_test"`,
`owner_id=1` (все — константы-примеры, заменяемые при интеграции).

### Специфика / сложности реализации

Перечисли всё, что неочевидно из spec/plan и важно помнить при сопровождении.
Примеры тем:

- **Кодировка ответа:** cp1251 → читаем через `encoding="cp1251"`; CSV сохраняем с `encoding="cp1251", errors="replace"` — без `errors="replace"` pandas молча пишет UTF-8, Excel на Windows открывает с кракозябрами
- **Разделитель CSV:** `;` (не `,`) — указывать `sep=";"` в `pd.read_csv`
- **Лимит диапазона дат:** API режет запросы > 62 дней — делим на чанки
- **Rate limit:** submit не чаще 1/20 сек — между запросами `time.sleep(20)`
- **Пагинация:** курсорная по `nextPageToken`; пустой `rows` означает конец
- **Пустой ответ:** API возвращает `204` вместо `200` — обрабатываем отдельно
- **Группировка:** при `groupBy=NO_GROUP_BY` отсутствуют поля `date`/`campaignId`,
  возвращается одна строка-агрегат
- **Расхождения с доками:** поле `X` в реальности приходит как string, не number
- **Источник `costs_nds`:** название колонки = "расход С НДС". Для BANNER берётся
  поле API «Расход, ₽, с НДС». Для VIDEO_BANNER API возвращает только «Расход, ₽»
  (без НДС) — в этом случае `costs_nds` фактически содержит сумму БЕЗ НДС;
  обязательно зафиксировать это здесь

### История изменений

| Дата | Изменение | Причина |
|------|-----------|---------|
| YYYY-MM-DD | Первичная реализация | — |
| YYYY-MM-DD | Добавлено поле `clicks` | API стал возвращать новое поле |
| YYYY-MM-DD | Сменили endpoint v1 → v2 | Старый задепрекейчен |

---

<повторить блок для каждой реализованной функции>
