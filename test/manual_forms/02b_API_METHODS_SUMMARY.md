# Сводка методов API — {API_NAME}

Этот файл — **шаблон/образец оформления**. На Шаге 1.5 Claude изучает
документацию API (URL из A3 анкеты `02_NEW_PROJECT_QUESTIONNAIRE.md`) и
создаёт по этому образцу файл **`info/00_api_methods.md`** в корне проекта
(папку `info/` создать, если её ещё нет).

**Цель:** зафиксировать только те методы, которые относятся к **получению статистики**
и к **аутентификации (выпуск/обновление токенов)**. Методы создания/редактирования
рекламных кампаний, управления объявлениями, биддинга и т.п. — **не включаются**.

**Источник правды:** только официальная документация ({DOCS_URL}). Поля и типы —
оригинальные, как в доках, без перевода и переименования.

---

## 1. Аутентификация — выпуск и обновление токенов

Перечисли все методы, которые отвечают за получение/обновление access-токена
(OAuth2 client credentials, refresh token, API-key exchange и т.п.).

### Метод: `{HTTP_METHOD} {ENDPOINT_PATH}`
- **Назначение:** <выпуск access-токена / обновление / отзыв>
- **Тип авторизации:** <client_credentials / refresh_token / api_key / ...>
- **Формат запроса:** <application/json / application/x-www-form-urlencoded>
- **Параметры запроса (оригинальные имена):**
  | Поле | Тип | Обязательное | Описание |
  |------|-----|-------------|----------|
  | `client_id` | string | да | <...> |
  | `client_secret` | string | да | <...> |
  | `grant_type` | string | да | <...> |
- **Формат ответа:** `application/json`
- **Поля ответа (оригинальные имена):**
  | Поле | Тип | Описание |
  |------|-----|----------|
  | `access_token` | string | <...> |
  | `expires_in` | integer (секунды) | <...> |
  | `token_type` | string | <...> |
- **TTL токена / частота обновления:** <напр. 30 минут>
- **Ограничения / rate limits:** <если есть>

<повторить блок для каждого метода аутентификации>

---

## 2. Методы получения статистики

Перечисли **все** методы, которые возвращают статистические данные
(показы, клики, расходы, охваты, конверсии, отчёты и т.п.).

Для каждого метода — отдельный блок ниже.

### Метод: `{HTTP_METHOD} {ENDPOINT_PATH}`
- **Название в доках:** <как названо в документации>
- **Назначение:** <короткое описание, какую статистику возвращает>
- **Тип:** <синхронный отчёт / асинхронный (submit + poll + download) / стриминг>
- **Группировка / разрезы:** <по дням / по кампаниям / NO_GROUP_BY / ...>
- **Параметры запроса (оригинальные имена):**
  | Поле | Тип | Обязательное | Описание / допустимые значения |
  |------|-----|-------------|-------------------------------|
  | `dateFrom` | string (YYYY-MM-DD) | да | <...> |
  | `dateTo` | string (YYYY-MM-DD) | да | <...> |
  | `campaigns` | array[string] | нет | <...> |
  | `groupBy` | string | нет | `DATE` / `NO_GROUP_BY` / ... |

- **Формат ответа:** <`application/json` / `text/csv` / `text/tab-separated-values` / `application/zip` (с CSV/TSV внутри) / ...>
- **Кодировка:** <UTF-8 / UTF-8 BOM / windows-1251 / ...>
- **Разделитель (для CSV/TSV):** <`,` / `;` / `\t`>
- **Структура ответа:**
  - Если **JSON** — таблица полей:
    | Поле | Тип | Описание |
    |------|-----|----------|
    | `rows` | array[object] | <...> |
    | `rows[].date` | string | <...> |
    | `rows[].views` | integer | <...> |
    | `rows[].moneySpent` | number (decimal) | <...> |
  - Если **CSV/TSV** — список колонок в исходном порядке:
    | № | Колонка (оригинал) | Тип | Описание |
    |---|-------------------|-----|----------|
    | 1 | `Дата` / `Date` | string (YYYY-MM-DD) | <...> |
    | 2 | `Показы` / `Views` | integer | <...> |
    | 3 | `Расход, ₽` / `MoneySpent` | number | <...> |
  - Если **ZIP** — что внутри (имена файлов, формат каждого).

- **Особенности / ограничения:**
  - Максимальный диапазон дат: <напр. 62 дня>
  - Rate limits: <напр. 1 запрос/20 сек на submit>
  - Лимит на количество кампаний в одном запросе: <...>
  - Поведение при пустом результате: <пустой массив / 204 / ...>

- **Связанные методы (для асинхронных):**
  - submit: `POST /...`
  - status: `GET /.../{uuid}`
  - download: `GET /.../{uuid}/download`

<повторить блок для каждого метода статистики>

---

## 3. Сводная таблица методов статистики

| # | Endpoint | Назначение | Формат ответа | Sync/Async | Ключевые поля ответа |
|---|----------|------------|---------------|------------|---------------------|
| 1 | `POST /api/client/statistics` | Дневная статистика по кампаниям | ZIP→CSV | async | `date`, `views`, `clicks`, `moneySpent` |
| 2 | ... | ... | ... | ... | ... |

---

## 4. Что НЕ включено (явно)

Перечисли категории методов, которые встретились в доках, но сознательно
исключены из этой сводки (для прозрачности).

- Методы создания/редактирования рекламных кампаний (`POST /campaign`, ...)
- Методы управления объявлениями и креативами
- Методы управления ставками и биддингом
- Методы управления товарами/каталогом
- <добавь категории, специфичные для {API_NAME}>
