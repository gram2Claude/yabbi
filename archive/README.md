# Архив

Код, выведенный из активной библиотеки, но сохранённый рабочим на случай возврата.

| Файл | Что | Когда/почему |
|------|-----|--------------|
| `get_reach_cumulative.py` | Накопительный охват по кампаниям (`reach`/`increment` из `amountIFA`, метод `campaigns-statistics`) + fetch-обёртка `fetch_campaigns_statistics_total` | 2026-07-20 — решение пользователя: «пока не нужен». Знание об источнике сохранено в `info/00_yabbi_source.md` §5.4 (SSOT). |

Как восстановить — см. докстринг соответствующего модуля.
